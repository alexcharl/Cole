;(function ( window, $) {
	var pumkin = window.pumkin = window.pumkin || {};
	var SITE = window.SITE = window.SITE || {};

	// ON DOC READY
	$(function () {
		
		SITE.initGlobal();

		SITE.initMain();

	});

})( this, this.jQuery );